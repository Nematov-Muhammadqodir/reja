<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>REJA</title>
    <link rel="stylesheet" href="../public/main.css" />
    <style>
      .container {
        display: flex;
        font-family: sans-serif;
        flex-direction: column;
        align-items: center;
      }

      .input {
        border: none;
        padding: 10px 15px;
        border-radius: 5px;
        background-color: rgb(220, 209, 209);
        color: brown;
        font-family: 1.5rem;
      }

      .btn {
        padding: 10px 15px;
        border: none;
        font-size: 1rem;
        color: rgb(197, 166, 120);
        border-radius: 5px;
      }

      .btn:hover {
        cursor: pointer;
      }
    </style>
  </head>
  <body style="background-color: rgb(197, 166, 120)">
    <div class="container">
      <h1 class="header" style="font-weight: 500">REJA VA MAQSADLAR</h1>
      <form id="create-form" class="form" action="/create-item" method="POST">
        <div>
          <input autofocus class="input" type="text" name="item" />
          <button class="btn">Yangi harid qoshish</button>
        </div>
      </form>
    </div>
  </body>
</html>

<!-- This input works because of: app.use(express.urlencoded({extended:true})) -->
